import 'package:flutter/material.dart';

class Consts {
  Consts._();

  static const double padding = 16.0;
  static const double avatarRadius = 66.0;

  // global
  static const Color backgroundColor = Color(0xFFEAEAEA);

  // operators dialog
  static const double operatorsDialogPadding = 16.0;
  static const Color operatorsBackgroundColor = Color(0xFF444444);
  static const Color operatorsTextColor = Colors.white;
}